<!DOCTYPE html>
<html lang="ja">
<head>
  <meta charset="UTF-8">
</head>

<body>
<form action="/confirm_edit/<%= @memo["id"].to_s %>" accept-charset="UTF-8" method="post">
  <input type="hidden" name="_method" value="PATCH">
  <div>
    <label class="form-item__label">タイトル</label>
    <input class="form-item_textarea" type="text" name="title" value="<%= @memo["title"].force_encoding("UTF-8") %>">
  <div>
  </div>
    <label class="form-item__label">内容</label>
    <textarea name="content" class="form-item_textarea"><%= @memo["content"].force_encoding("UTF-8") %></textarea>
  </div>
  <input type="submit" value="完了">
</form>
</body>